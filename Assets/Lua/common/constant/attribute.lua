constant.HERO_SHOW_ATTR = {
    -- cfg.attribute.power,
    cfg.attribute.maxHp,
    cfg.attribute.atk,
    cfg.attribute.moveSpeed,
    cfg.attribute.atkRange,
    -- cfg.attribute.skillLevel,
}

constant.INSTITUE_SOLDIER_SHOW_ATTR = {
    cfg.attribute.maxHp,
    cfg.attribute.atk,
    cfg.attribute.race,
    -- cfg.attribute.trainNeedStarCoin,
}

constant.INSTITUE_MINE_SHOW_ATTR = {
    cfg.attribute.maxHp,
    cfg.attribute.atk,
    cfg.attribute.trainNeedStarCoin,
}

constant.WARSHIP_SHOW_ATTR = {
    cfg.attribute.quantumPotential,
    cfg.attribute.durability,
    cfg.attribute.skillPoint,
    -- cfg.attribute.power
    -- cfg.attribute.tonnage,
    -- cfg.attribute.flyTime,
}

constant.SOLDIER_INFO_ATTR = {
    cfg.attribute.maxHp,
    cfg.attribute.atk,
    cfg.attribute.atkRange,
    cfg.attribute.moveSpeed,
    cfg.attribute.trainSpace,
    cfg.attribute.trainNeedTick,
}

constant.BUILD_SHOW_ATTR ={
    cfg.attribute.maxHp, 
    cfg.attribute.atk, 
    cfg.attribute.atkRange 
    }

constant.SOLDIER_SHOW_ATTR = {
    cfg.attribute.maxHp, 
    cfg.attribute.atk, 
    cfg.attribute.atkRange, 
    cfg.attribute.moveSpeed
}



